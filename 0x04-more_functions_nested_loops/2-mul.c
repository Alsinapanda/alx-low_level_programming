#include "main.h"
#include <stdio.h>

/**
 * mul - Multiply two integers
 * @a:First integer
 * @b:Second integer
 * Return: 0 (Success)
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
